{% extends 'base.html.twig' %}

{% block title %}Formulaire{% endblock %}

{% block body %}

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Add category</button>
    {{ form_end(form) }}
    
{% endblock %}