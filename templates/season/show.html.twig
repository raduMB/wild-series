{% extends 'base.html.twig' %}

{% block title %}Season
{% endblock %}

{% block body %}
	<h1>Season</h1>

	<table class="table table-dark table-hover table-borderless">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ season.id }}</td>
			</tr>
			<tr>
				<th>Number</th>
				<td>{{ season.number }}</td>
			</tr>
			<tr>
				<th>Year</th>
				<td>{{ season.year }}</td>
			</tr>
			<tr>
				<th>Description</th>
				<td>{{ season.description }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_season_edit', {'id': season.id}) }}" class="btn btn-secondary my-2 mx-4">Modifier</a>

	{{ include('season/_delete_form.html.twig') }}

	<a href="{{ path('app_season_index') }}" class="btn btn-info my-2 mx-4">Retour à la liste des seasons</a>
{% endblock %}
