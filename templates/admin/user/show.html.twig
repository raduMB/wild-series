{% extends 'admin/base.html.twig' %}

{% block title %}User
{% endblock %}

{% block body %}
	<h1>Utilisateur</h1>

	<table class="table table-dark table-hover table-borderless">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ user.id }}</td>
			</tr>
			<tr>
				<th>Email</th>
				<td>{{ user.email }}</td>
			</tr>
			<tr>
				<th>Username</th>
				<td>{{ user.username }}</td>
			</tr>
			<tr>
				<th>Bio</th>
				<td>{{ user.bio }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('admin_user_edit', {'id': user.id}) }}" class="btn btn-secondary my-2 mx-4">Modifier</a>

	{{ include('admin/user/_delete_form.html.twig') }}

	<a href="{{ path('admin_user_index') }}" class="btn btn-info my-2 mx-4">Retour à la liste des utilisateurs</a>
{% endblock %}
